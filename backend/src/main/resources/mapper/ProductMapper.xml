<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wms.backend.mapper.ProductMapper">

    <!-- 물류 등록   -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product(
            sku,
            name,
            price,
            current_stock
        ) VALUES(
            #{sku},
            #{name},
            #{price},
            #{currentStock}
        )
    </insert>

    <!-- 물류 전체 조회   -->
    <select id="findAll" resultType="com.wms.backend.entity.Product">
        SELECT
            id,
            sku,
            name,
            price,
            current_stock,
            created_at,
            updated_at
        FROM product
        ORDER BY id ASC
    </select>

    <!-- 물류 단일 조회   -->
    <select id="findById" resultType="com.wms.backend.entity.Product">
        SELECT
            id,
            sku,
            name,
            price,
            current_stock,
            created_at,
            updated_at
        FROM product
        WHERE id = #{id}
    </select>

    <!-- 물류 sku 조회  -->
    <select id="findBySku" resultType="com.wms.backend.entity.Product">
        SELECT *
        FROM product
        WHERE sku = #{sku}
    </select>

    <!-- 물류 수정   -->
    <update id="update">
        UPDATE product
        SET
            name=#{name},
            price=#{price},
            current_stock=#{currentStock},
            updated_at=NOW()
        WHERE
            id = #{id}

    </update>

    <!-- 물류 삭제   -->
    <delete id="delete">
        DELETE FROM product
        WHERE
            id = #{id}
    </delete>

    <!--  전체 조회 + 검색 + 정렬 + 페이징  -->
    <select id="findProducts" resultType="com.wms.backend.entity.Product">
        SELECT *
        FROM product

        <where>
            <if test="keyword !=null and keyword!='' ">
                (
                    LOWER(name) LIKE CONCAT('%',LOWER(#{keyword}),'%')
                    OR
                    LOWER(sku) LIKE CONCAT('%',LOWER(#{keyword}),'%')
                )
            </if>
        </where>

        <choose>
            <when test="sortColumn != null and sortDir != null ">
                ORDER BY ${sortColumn} ${sortDir}
            </when>
            <otherwise>
                ORDER BY id ASC
            </otherwise>
        </choose>

        <!--   ex) LIMIT 10 OFFSET 5 = > 5개 건너 뛰고  10(6~15)개 가져오기    -->
        LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- 물류 개수 조회 (페이징용) -->
    <select id="countProducts" resultType="int">
        SELECT COUNT(*)
        FROM product
        <where>
            <if test="keyword != null and keyword != ''">
                (
                LOWER(name) LIKE CONCAT('%', LOWER(#{keyword}), '%')
                OR LOWER(sku) LIKE CONCAT('%', LOWER(#{keyword}), '%')
                )
            </if>
        </where>
    </select>


<!--  해당 재고  where 조건문에 일치하는게 없다면 0을 반환  -->
    <!-- 재고 증가  -->
    <update id="increaseStock">
        UPDATE product SET
        current_stock = current_stock + #{quantity} ,
        updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 재고 감소  -->
    <update id="decreaseStock">
        UPDATE product SET
        current_stock = current_stock - #{quantity} ,
        updated_at = NOW()
        WHERE id = #{id} AND current_stock >= #{quantity}
    </update>

    <!-- 현재 등록된 물류 개수   -->
    <select id="totalProductCount" resultType="int">
        SELECT count(*)
        FROM product
    </select>

    <!-- 현재 물류 총량   -->
    <select id="nowStockCount" resultType="int">
        SELECT SUM(current_stock)
        FROM product
    </select>







</mapper>